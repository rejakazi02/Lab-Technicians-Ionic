import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.app.redgroccer',
  appName: 'Red groccer',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
